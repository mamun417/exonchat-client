import { MutationTree } from 'vuex';
import { ChatTemplateStateInterface } from './state';

const mutation: MutationTree<ChatTemplateStateInterface> = {
    someMutation(/* state: ChatStateInterface */) {
        // your code
    },
};

export default mutation;
