import { MutationTree } from 'vuex';
import { IntentStateInterface } from './state';

const mutation: MutationTree<IntentStateInterface> = {
    someMutation(/* state: ChatStateInterface */) {
        // your code
    },
};

export default mutation;
