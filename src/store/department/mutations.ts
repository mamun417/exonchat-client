import { MutationTree } from 'vuex';
import { DepartmentStateInterface } from './state';

const mutation: MutationTree<DepartmentStateInterface> = {
    someMutation(/* state: ChatStateInterface */) {
        // your code
    },
};

export default mutation;
