import { MutationTree } from "vuex";
import { UserInvitationStateInterface } from "./state";

const mutation: MutationTree<UserInvitationStateInterface> = {
    someMutation(/* state: ChatStateInterface */) {
        // your code
    },
};

export default mutation;
